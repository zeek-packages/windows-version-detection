[package]
description = Zeek package to detect the Windows version via access to Microsoft CRLs
credits = Vlad Grigorescu <vlad@es.net>
tags = software, windows, fingerprinting

test_command = cd tests && make

script_dir = ./scripts

depends =
  zeek >=2.0.0